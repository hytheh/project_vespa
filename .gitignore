# PlatformIO (Microcontrollers)
.pio

# VSCode - Share configuration, ignore local state
.vscode/.browse.c_cpp.db*
.vscode/ipch/
.vscode/c_cpp_properties.json # Often local-path dependent, but can be shared if paths are relative
# We KEEP launch.json and tasks.json to share build workflows

# C++ / CMake (Jetson)
build/
builds/
bin/
lib/
*.o
*.so

# Python
__pycache__/
*.pyc

# OS / IDE
.DS_Store
.idea/
*.swp
*.CATSystem
secrets.h
.env